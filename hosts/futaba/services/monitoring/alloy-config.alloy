prometheus.remote_write "default" {
  endpoint {
    url = "https://mimir.local.triserden.dev/api/v1/push/"
  }
}

prometheus.scrape "node-exporter" {
  targets = [{
    __address__ = "node-exporter:9100",
  },]
  forward_to = [prometheus.remote_write.default.receiver,]
}
